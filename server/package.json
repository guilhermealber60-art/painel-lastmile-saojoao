{
  "name": "lastmile-dashboard-api",
  "version": "1.0.0",
  "private": true,
  "description": "API de persistência (PostgreSQL) do painel Last Mile — substitui o publish via GitHub.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "migrate": "node scripts/migrate.js",
    "import-snapshot": "node scripts/import-snapshot.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
